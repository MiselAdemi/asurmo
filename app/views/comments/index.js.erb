$('#comment-container-' + <%= params[:activity_id] %>).append('<%= escape_javascript(render partial: "comments/reply", locals: {comments: @comments, new_comment: Comment.build_from(Activity.find(params[:activity_id]).trackable, current_user.id, ""), type: :root }) %>')
