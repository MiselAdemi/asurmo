<li>
  <div class="timeline-badge">
  	<%= image_tag activity.user.avatar.url %>
  </div>
  <div class="timeline-panel">
    <div class="timeline-heading">
      <h4 class="timeline-title"><%= activity.user.full_name %></h4>
      <p><small class="text-muted" ><i class="glyphicon glyphicon-time"></i>
      <time class="timeago" datetime="<%= activity.created_at.localtime.strftime '%Y-%m-%d %H:%M:%S' %>"></time>
      </small></p>
    </div>
   
    <%= render :partial => "activities/partials/#{ activity.trackable_type.downcase }", :locals => { :activity => activity } %>

    <hr>
    <a onclick="toggler(<%= activity.id %>);">
    	<small><%= activity.trackable.comment_threads.count %> <i class="fa fa-comments-o" aria-hidden="true"></i></small>
    </a>
    <%= sync partial: 'timeline_support_box', resource: activity %>

    <div class="comment-box comment-box-<%= activity.id %>" >
    <%= render partial: "comments/template", locals: {commentable: activity.trackable, new_comment: Comment.build_from(activity.trackable, current_user.id, "") } %>
  	</div>
  </div>
</li>