<%= link_to 'Join', organization_join_member_path(@organization, current_user.id), :method => :post, :class => "bg-transparent no-underline hover:bg-blue text-blue-dark font-semibold hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded" if !is_supporting_organization?(@organization, current_user) %>

<%= link_to 'Exit organization', organization_remove_member_path(@organization, current_user.id), :method => :delete, :remote => true, :class => "bg-transparent no-underline hover:bg-blue text-blue-dark font-semibold hover:text-white py-2 px-4 border border-blue hover:border-transparent rounded" if is_member?(@organization, current_user) %>
