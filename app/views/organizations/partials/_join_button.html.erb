<%= link_to 'Join', organization_join_members_path(@organization, current_user.id), :method => :post, :class => "btn btn-success join-btn" if !is_supporting_organization?(@organization, current_user) %>

<%= link_to 'Exit organization', organization_remove_member_path(@organization, current_user.id), :method => :delete, :class => "btn btn-default join-btn" if is_member?(@organization, current_user) %>
