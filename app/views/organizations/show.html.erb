<p id="notice"><%= notice %></p>

<p>
<strong>Name:</strong>
<%= @organization.name %>
</p>


<%= link_to 'Edit', edit_organization_path(@organization) %>
<%= link_to 'Back', organization_path %>
<%= link_to 'Add moderator', organization_moderators_path(@organization) unless @organization.user_id == current_user.id %>
